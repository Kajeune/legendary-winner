

name: Build Popi AAB

on:
  push:
    branches: ["main"]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository 
        uses: actions/checkout@v4
      
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
           distribution: 'temurin'
           java-version: '17'

      - name: Decode keystore
        run: echo "$
{{ Secrets.KEYSTORE_BASE64 }}: base64
--decode > my-release-key.jks

       - name: Build Signed AAB (Popi)
         run: |
         ./gradlew :Popi:bundleRelease\

-Pandroid.injected.signing.store.file=my-re
 lease-key.jks \

-Pandroid.injected.signing.store.password=$
{{ Secrets.KEYSTORE_PASSWORD }} \

-Pandroid.injected.signing.key.alias=$
{{ Secrets.KEY_ALIAS }} \

-Pandroid.injected.signing.key.password=$
{{ Secrets.KEY_PASSWORD }}

      -name: Upload Signed AAB
       uses: actions/upload-artifact@v4
       with
           name: Popi-release
           path: Popi/build/outputs/
bundle/release/*.aab
